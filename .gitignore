# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
# remove .next so i can build locally maybe ??
/.next/ 
/out/

# production
# DO I NEED TO REMOVE THIS TO ?
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Both under added based on chatGpt
# ─── Turbo (Monorepos, CI optimization - Optional) ───────────
/turbo            # Turbo cache dir, can skip on server
.cache/           # If you use Turborepo caching

# ─── VS Code (Optional) ──────────────────────────────────────
.vscode/
settings.json     
# Only ignore if you're not sharing workspace settings
# remove for laptop push then remove file and add this to gitignore on server